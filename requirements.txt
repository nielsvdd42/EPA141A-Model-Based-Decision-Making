# ── JUSTICE core dependencies (subset — excludes RL/ML stack not used in A1-A8)
fair==2.1.3
h5py>=3.10.0
platypus-opt>=1.1.0
ema-workbench>=3.0
scipy>=1.10.0
deap>=1.4.0
ipyparallel>=8.6.1
plotly>=5.19.0
pycountry>=24.6.1

# ── Assignments 1-8 ──────────────────────────────────────────────────────────
numpy>=1.24
pandas>=1.5
matplotlib>=3.7
seaborn>=0.13
SALib>=1.4          # A2: sensitivity analysis
geopandas>=0.14     # A7: world maps
tqdm>=4.0           # A1, A2, A3, A8: progress bars

# ── Notebook environment ──────────────────────────────────────────────────────
jupyterlab>=4.0
ipywidgets>=8.0
kaleido>=0.2        # plotly static image export (A7 maps)
# altair — DO NOT install on Python 3.14+
#   altair ≥6.0 uses TypedDict(closed=True) which raises TypeError (not ImportError) on
#   Python 3.14, bypassing the try/except guard in ema_workbench/analysis/prim.py and
#   breaking *all* of ema_workbench.analysis. ema_workbench[graph] pulls it in transitively.
#   Fix if accidentally installed: pip uninstall altair
#   ema_workbench falls back to matplotlib for prim.inspect_tradeoff() without altair.
